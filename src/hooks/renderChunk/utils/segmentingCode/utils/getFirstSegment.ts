/**
 * Get the first code segment.
 * @param segments The segmented codes
 * @returns The first code segment
 */
const getFirstSegment = (
  segments: string[]
): string => {
  // Get the first code segment.
  const firstSegment = segments[0]

  // Return the first code segment.
  return firstSegment
}

export default getFirstSegment
